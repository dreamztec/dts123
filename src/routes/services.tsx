import { createFileRoute } from '@tanstack/react-router'; import { PublicPage } from '@/components/PublicPage'; import { publicPages } from '@/lib/public-pages'; export const Route=createFileRoute('/services')({component:()=> <PublicPage {...publicPages.services}/>})
