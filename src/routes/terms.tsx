import { createFileRoute } from '@tanstack/react-router'; import { PublicPage } from '@/components/PublicPage'; import { publicPages } from '@/lib/public-pages'; export const Route=createFileRoute('/terms')({component:()=> <PublicPage {...publicPages.terms}/>})
